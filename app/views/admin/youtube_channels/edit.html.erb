<% content_for(:title, "Edit Channel — Boaner Admin") %>

<div class="mb-8">
  <h1 class="text-2xl font-bold text-gray-900">Edit Channel</h1>
  <p class="text-gray-500 text-sm mt-1">Update settings for <%= @channel.name %>.</p>
</div>

<div class="max-w-2xl">
  <%= render "form", channel: @channel %>
</div>

<% content_for(:title, "Edit Series — Boaner Admin") %>

<div class="mb-8">
  <a href="<%= admin_series_index_path %>" class="text-sm text-gray-500 hover:text-amber-600 font-medium inline-flex items-center mb-2">
    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" /></svg>
    Back to Series
  </a>
  <h1 class="text-2xl font-bold text-gray-900">Edit Series</h1>
</div>

<div class="max-w-3xl">
  <%= render "form", series: @series %>
</div>

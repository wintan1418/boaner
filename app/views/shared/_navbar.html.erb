<nav class="bg-white border-b border-ink-100 sticky top-0 z-40" data-controller="navbar">
  <div class="max-w-5xl mx-auto px-4 sm:px-6">
    <div class="flex items-center justify-between h-14">
      <%= link_to root_path, class: "text-lg tracking-tight font-bold text-ink-900 hover:text-accent transition-colors", style: "font-family: 'Source Serif 4', Georgia, serif;" do %>
        boaner
      <% end %>

      <div class="hidden md:flex items-center gap-6 text-sm">
        <%= nav_link "Videos", videos_path %>
        <%= nav_link "Blog", posts_path %>
        <%= nav_link "Courses", courses_path %>
        <%= nav_link "Books", books_path %>
        <%= nav_link "About", about_path %>
        <%= nav_link "Contact", contact_path %>
      </div>

      <button data-action="click->navbar#toggle" class="md:hidden p-2 -mr-2 text-ink-500 hover:text-ink-800" aria-label="Menu">
        <svg data-navbar-target="openIcon" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
        <svg data-navbar-target="closeIcon" class="w-5 h-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
    </div>
  </div>

  <div data-navbar-target="menu" class="hidden md:hidden border-t border-ink-100">
    <div class="px-4 py-2 space-y-1">
      <%= mobile_nav_link "Videos", videos_path %>
      <%= mobile_nav_link "Blog", posts_path %>
      <%= mobile_nav_link "Courses", courses_path %>
      <%= mobile_nav_link "Books", books_path %>
      <%= mobile_nav_link "About", about_path %>
      <%= mobile_nav_link "Contact", contact_path %>
    </div>
  </div>
</nav>
